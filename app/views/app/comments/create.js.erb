$(document).ready(function(){
  $('#comments-list').prepend("<%= j(render('comment', :comment => @comment)) %>").hide().slideDown();
  $('#comment_message').val("");
  $('#comments-list').focus();
});
